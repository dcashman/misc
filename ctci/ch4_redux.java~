import dataStructs.*;
import java.util.Random;

public class ch4_redux{

    //balanced means that the height of every node is no more than 1 different than that of its siblings, return -1 if not balanced
    public static int isBalanced(BinaryTreeNode n){
	if(
	


    public static void main(String[] args){
        Random randerr = new Random();
	BinarySearchTreeNode newTree = new BinarySearchTreeNode(4);
	int temp;
        for(int i=0; i<20; i++){
            temp=randerr.nextInt(20);
            newTree.add(temp);
	    System.out.print(temp+", ");
	}
	System.out.print("\n"+newTree.toStringInOrder()+"\n");
	return;
    }
}